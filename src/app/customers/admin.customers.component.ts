import { Component } from '@angular/core';

@Component({
    selector: '.customersComp',
    template: `
        customers
    `
})
export class CustomersComponent {
// TODO: MOVE TO ADMIN FOLDER & move everything else up one level
}
